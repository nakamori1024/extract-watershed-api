[project]
name = "watershed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.67",
    "numpy>=2.5.1",
    "pillow>=12.3.0",
    "pydantic>=2.13.4",
    "rasterio>=1.5.1",
    "s3fs>=0.4.2",
    "zarr[remote]>=3.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
